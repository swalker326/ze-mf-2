/**
 * Checks that obj has a deeply nested property with value usage:
 * `objHasKeys(federationRemoteEntry, ["origins", "0", "loc"])`
 *
 * @param obj
 * @param pathArr
 */
export declare function objHasKeys<T extends Record<string, T[keyof T] | T>>(obj: T, pathArr: string[]): boolean;
