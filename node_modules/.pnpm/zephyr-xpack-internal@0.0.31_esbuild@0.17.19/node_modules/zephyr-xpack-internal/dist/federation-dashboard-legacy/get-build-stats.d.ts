import { type ConvertedGraph } from 'zephyr-edge-contract';
import { ZephyrEngine } from 'zephyr-agent';
import { ModuleFederationPlugin, XStats, XStatsCompilation } from '../xpack.types';
interface KnownAgentProps {
    stats: XStats;
    stats_json: XStatsCompilation;
    pluginOptions: {
        zephyr_engine: ZephyrEngine;
        mfConfig: ModuleFederationPlugin[] | ModuleFederationPlugin | undefined;
    };
}
export declare function getBuildStats<ZephyrAgentProps extends KnownAgentProps>({ stats, stats_json, pluginOptions, EDGE_URL, DOMAIN, PLATFORM, TYPE, }: ZephyrAgentProps & {
    EDGE_URL: string;
    DOMAIN?: string;
    PLATFORM?: string;
    TYPE?: string;
}): Promise<ConvertedGraph>;
export {};
